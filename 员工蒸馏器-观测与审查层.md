# 员工蒸馏器 · 观测与审查层（Pull 模式）

> 补一个之前缺失的设计维度：人**怎么看库、看调用效果、做审查**。
> 与"运维层"并列。核心原则：**审查是 pull，不是 push**——系统默默跑，人主动来看才看到，永不弹窗、永不催办。
> 配套 v0.4（§3.1 无感 / §5.4 价值确认 / §6 人工 pin / §9.1 iteration_log）、运维层。
> 日期：2026-06-20

---

## 0. 为什么需要这一层

之前的设计反复提"人工 pin / 异步抽查 / 可观测 / Obsidian 视图"，但**从没定义人用的入口**：库里有什么工具、调用效果如何、待审的东西在哪、怎么干预——全是空的。

一个号称"沉淀员工能力"的系统，如果人无法查看沉淀了什么、好不好用、也无法纠偏，它就是个**不可信的黑箱**。这一层把黑箱开一扇"人想看才打开"的窗。

---

## 1. 第一原则：Pull，不是 Push

| | Push（我们不要） | Pull（我们要） |
|---|---|---|
| 触发 | 系统主动弹"有 3 个待审" | 人主动 `distiller pending` 才看到 |
| 打扰 | 破坏无感 | 零打扰 |
| 时机 | 系统决定 | 人决定 |

**运行时无感 + 审查 pull = 两者不打架**：daemon 后台静默跑，不打断你；你想看时主动开 Obsidian / 敲 CLI，一切尽收眼底。

> 边界（不绝对化）：**普通审查 pull-only**（ambiguous/Path-B/误合并复核，永不弹待办）；但**严重健康/安全事故**（磁盘满、采集 drop、疑似 secret 泄漏、SQLite 损坏）**允许低噪声暴露**（health 状态 / CLI banner / `reports/health.json`），不打断前台、不弹审查待办，但**不能"不报告事故"**。

### 1.1 Pull 模式的硬推论：待审不能阻塞主流程

既然没人保证会来 pull，**任何"等人审"的东西都必须有默认安全行为，不能卡死**：

| 待审项 | 默认行为（人来之前） | 人来之后可改 |
|---|---|---|
| ambiguous（迭代/重复判不出） | **并列保留** retained_impls；旧实现保持 `stable_active`（人审前**不替换**），新实现进 `provisional`（沙箱影子评估，**不进 MCP、不替换 active**） | 人审或证据足够 → promote-impl |
| Path-B 高价值候选 | 留在 candidate，**不自动转正**（保守，不污染库） | 人 pin → 转正 |
| 疑似误合并 | **冻结影响面**：冻新暴露 + 冻 shared 提级 + active 回退到 pre-merge；合并快照保留可回退（不放任扩大污染） | 人确认 → 拆回或确认 |

> 原则：默认行为永远偏"保守、可回退、不丢年轮"。pull 没发生 = 系统停在安全态，而不是停在卡死态。

---

## 2. 看有什么：库浏览

### 2.1 Obsidian 视图（人友好，最轻）

manifest 已是 markdown（v0.4 §9.1），**直接 Obsidian over `skills/`** 就是现成库浏览器：

- 每个能力一张卡：purpose / 分支列表 / 共享后处理 / 质量分 / 复用次数
- `iteration_log` + git log → 年轮一目了然（这工具怎么长出来的）
- `lineage` 双链 → 合并/拆分/父子关系可视
- 纯只读浏览，不经过它做存储或写入（它只是视图）

### 2.2 CLI（封闭环境兜底）

不开 Obsidian 也能在终端查：

```
distiller ls                    # 列所有能力：name / 分支数 / 复用 / 成功率 / 状态
distiller show doc-to-markdown  # 单个能力详情：契约 / 分支 / 年轮 / lineage
distiller branches doc-to-markdown   # 看分支与 active/retained
```

---

## 3. 看好不好用：调用效果

数据**早就在采集**（`usage_events` + `quality_rollup`，与 `iteration_log` **分开记**——别拿每次调用污染年轮），只是没有面向人的呈现。这一层把它显出来：

| 看什么 | 来源 | CLI |
|---|---|---|
| 被哪些项目调过、成功/失败 | 迭代日志 | `distiller usage <skill>` |
| 最近成功率趋势 | quality 滚动分 | 同上 |
| warm-start 命中/未命中 | 回灌记录 | `distiller warmstart-stats` |
| 在什么输入上栽过 | R_miss 样本 | `distiller failures <skill>` |
| 行为指纹（输出手艺） | behavior_signature | `distiller show` 内 |

> 这回答了你的"完全封闭运行怎么看调用效果"：效果数据一直在记，CLI/Obsidian 是 pull 出来看的窗口。

---

## 4. 人能干预：审查队列与动作

### 4.1 审查队列（pull）

把散落的人工点收成一个**待办**，人主动查：

```
distiller pending          # 列出所有待人定夺项：
                           #  - ambiguous: judge 判不出迭代/重复
                           #  - path-b:    高价值候选待 pin
                           #  - review:    疑似误合并待确认
```

队列只在你敲命令时出现，**不主动通知**。每项附齐决策所需信息（两个实现的 behavior_signature 对比、契约、各自 fixture 表现），让人**能判**而不是干瞪眼。

### 4.2 审查动作

```
distiller pin <candidate>           # 高价值候选转正（Path-B）
distiller resolve <id> --iteration  # ambiguous 定为迭代
distiller resolve <id> --keep-both  # ambiguous 定为并列保留
distiller deprecate <skill|branch>  # 手动降级烂工具（不召回）
distiller unmerge <skill>           # 拆回误合并（用已存快照）
distiller promote-impl <skill> <impl>  # 手动切 active 实现
```

所有动作**写 lineage**（v0.4 §3 原则8：人工干预也要可追溯），可回退。

---

## 5. 与无感、运维层的关系

| 层 | 朝向 | 模式 |
|---|---|---|
| 采集 hook | 机器 | 自动、无感 |
| daemon 流水线 | 机器 | 自动、空闲触发 |
| **观测与审查（本层）** | **人** | **pull，人主动** |
| 运维健康 | 机器/运维 | heartbeat + reports |

观测层读的是 daemon 已经产生的状态（SQLite + skills/ + reports/），**只读为主、写仅限人工审查动作**，不参与自动流水线，因此不影响无感与性能。

---

## 6. MVP 范围

MVP 不需要做花哨 UI，最小可用：

1. **`distiller ls` / `show` / `pending`**：看库 + 看待审（必须）
2. **`distiller usage` / `failures`**：看效果（必须，否则无法判断价值）
3. **`distiller pin / resolve / deprecate`**：最小干预（必须，否则纠不了偏）
4. Obsidian over `skills/`：可选加分（manifest 已是 md，零额外成本）

`unmerge / promote-impl` 等可放阶段二。

---

## 7. 一句话

审查是 **pull**：系统默默跑、永不 push，人想看时用 `distiller ls/show/usage/pending` 或 Obsidian 主动查库与效果，用 `pin/resolve/deprecate` 纠偏。Pull 的硬约束是**待审不阻塞**——没人来审时，一切停在"保守、可回退、不丢年轮"的安全默认态。这一层让"无感黑箱"变成"无感但随时可查可纠"的系统。
