# 员工蒸馏器 · 候选发现规则

> 主脊第一步：从 trace 事件流里判定"这段是个该沉淀的工具碎片"，而不是一次性零碎代码。
> 展开 `技术栈与运行时-canonical.md` §3.2 的"自写规则"与 `v0.4` §13.2 step2。
> 日期：2026-06-20

---

## 0. 定位：高召回提名器，不是精度过滤器

候选发现只干一件事：**从 trace 里提名"可能值得沉淀的工具碎片"**。它**不**负责：

| 不负责 | 谁负责 |
|---|---|
| 判"值不值得沉淀"（价值） | 属性 Output Gate + 持续复用 |
| 判"是否重复 / 该不该合并" | 契约引擎（三选一） |
| 判"是不是该转正"（可复用性） | Promotion（频次/跨项目） |
| 判"确定性等级" | Gate 0 |

> 一句话：**发现 = 召回，下游 = 精度。** 宁可多提名（generous），让 Gate 和 Promotion 去筛，也不要在入口处用"看起来像不像"误杀。这同时守住无感——发现必须便宜（守则①②）。

输入：`traces/` 的 `ToolCallEvent` 流。输出：一条 `candidate`（状态 `raw`），进入流水线。

---

## 1. 第一步是关联，不是判断：把碎片拼成一个候选

一个工具在 trace 里往往**散落成多条事件**：agent 写了 `conv.py`（`generated_code` / `file_edit`），然后跑它（`command_exec`），产生输入输出（artifacts）。发现的第一动作是**关联**：

关联键（强→弱）：
1. `task_id`（同一任务）
2. `cwd` + 脚本文件名（同一脚本的写入与调用）
3. 时间邻近 + artifact 路径衔接（上一步的 output 是下一步的 input）

关联产物 = **候选记录**：
```yaml
candidate:
  entry: { kind: script|command, ref: conv.py, argv: [...] }
  code_snapshot: <原样代码，带年轮>
  observed_io:
    inputs:  [{ path, media_type, hash }]
    outputs: [{ path, media_type, hash }]
  result: { status: success, exit_code: 0 }
  context: { task_id, cwd, source_ref }
```

> 关联做不准会污染后面一切——这是发现层真正的技术活，不是"shape 匹配"。

---

## 2. 形状过滤（MVP：doc→md 的强约束让发现可做）

MVP 范围把发现从"通用难题"压成"可判定"。只提名同时满足：

1. **单入口**：一个脚本 / 一条命令（不是多步内联流水线）
2. **文件进**：`observed_io.inputs` 非空，且 media_type ∈ 已识别文档类型（pdf/docx/xlsx/pptx/html…）
3. **md 出**：`observed_io.outputs` 含 `.md`（佐证：同时落了 `assets/` 图片旁产物）
4. **成功**：`result.status == success`
5. **有可辨入口**：能定位到可重放的入口（供 Replay Gate）

佐证信号（加分，非必需）：代码 import 了已知转换库（`fitz / docx / openpyxl / pptx / markitdown`）。

> 正是范围把"形状"卡死，发现才可做。一旦阶段二放开品类，形状过滤要换成更弱的"file-in → artifact-out + 可重放"通用形状，召回噪声会上升——那时更要靠下游 Gate 兜底。

---

## 3. 噪声预过滤（便宜地砍掉明显不是工具的）

在提名前，廉价规则先剔除明显噪声（只用已有字段，不跑重活）：

| 砍掉 | 信号 |
|---|---|
| 纯计算/探针 | 无文件 I/O，只 print / 返回值 |
| 项目私有胶水 | 输入是 repo 内的项目数据文件（非通用文档类型），或硬编码项目路径 |
| 一次性 REPL 试探 | 极短、无入口、未被再次调用 |
| 改动工作区 | `file_changes` 触及输出以外的工作区文件（疑似副作用脚本，非纯转换） |
| **调用已转正能力** | 命令是在调我们自己的 MCP tool / 已 promoted skill → 这是**复用事件**，喂 warm-start 指标，**不作新候选** |

> 注意最后一条：发现要能认出"这是在用已有工具"而非"在造新工具"，否则会把成功的 warm-start 误当成新碎片，污染库。

---

## 4. 粒度边界（在发现层就别提名长链）

发现层就要落实"最小单元=目的、不沉淀长链"（v0.4 §7）：

- **只提名干净的单入口**：一个脚本把"某文档类型 → md"做完。
- **多步流水线不拆、不提名**：若 trace 是"下载 → 转换 → 上传"的链，发现层**只把其中匹配形状的单步（转换那步）作为候选**，其余记为 `deferred:chain`，不进流水线（链拆解是阶段二/三）。
- **同脚本内多格式分支不拆**：`doc2md.py` 内部 pdf/docx 多分支是**一个候选**（一个入口、一个目的），交给契约引擎去识别分支结构，发现层不预先切开。

> 发现层的粒度原则：**按入口和目的提名，不按函数切碎，也不沿链展开。**

---

## 5. 分诊输出（generous，召回优先）

形状过滤 + 噪声预过滤后，每条候选打一个**粗分诊**（不是价值判断，只为排序/调度）：

- `strong`：形状全中 + import 已知转换库 + 成功 → 优先进流水线
- `weak`：形状大部中、但缺佐证（如未识别的 import、输出未被消费）→ 进流水线但低优先
- `thin_wrapper_suspect`：几乎只调一个开源库无自定义逻辑（ast-grep 初判）→ 提名但标记，契约引擎后续置 `is_thin_wrapper`
- `deferred`：链 / 非确定性疑似 / 关联不全 → 只记录，不进流水线

全部 `strong/weak/thin_wrapper_suspect` 都提名为 `candidate(raw)`。**宁滥勿缺**——下游会筛。

---

## 6. 与下游分工 & 冷启动

**分工链**（防止发现层越权）：
```
发现(召回) → Gate0(确定性) → Replay/属性Output(可运行+价值线索+behavior_signature)
→ 契约引擎(同目的?新分支?迭代?重复?) → Promotion(频次/跨项目→转正)
```

**冷启动**：库为空时，每个 doc→md 碎片都"新"。发现层照常 generous 提名，契约引擎把它们**聚成同一个正在生长的 `doc-to-markdown`**（第一个 pdf 碎片建 skill+pdf 分支，后续 docx 碎片成新分支……）——这正是 `doc2md` 从碎片长成终态的路径。

---

## 7. doc2md 走一遍（发现层视角）

1. 项目A：agent 写 `conv.py`（`fitz.open().get_text()`）→ 跑 → 输入 `a.pdf`、输出 `a.md`。
2. 关联：`generated_code(conv.py)` + `command_exec(python conv.py a.pdf)` + artifacts → 一条候选记录。
3. 形状过滤：单入口✓、pdf 进✓、md 出✓、成功✓、入口可定位✓ → 命中。
4. 噪声预过滤：有文件 I/O、输入是通用文档类型、未改工作区、非调已有 skill → 不砍。
5. 佐证：import `fitz` → 已知转换库 → 分诊 `strong`。
6. 提名 `candidate(raw)` → 进 Gate0。**发现层到此为止**，不判它通不通用、不查重——交给下游。

---

## 8. 一句话

候选发现是入口，原则是**便宜 + 高召回 + 按入口/目的提名**：用 MVP 范围把"形状"卡到可判定，用廉价预过滤砍明显噪声，把"值不值得/重不重复/能不能转正"全部下放。它做对的标志不是"提名得准"，而是**不漏真工具、不卡前台、不越权下判断**。
