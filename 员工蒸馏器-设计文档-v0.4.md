# 员工蒸馏器 · 设计文档 v0.4（契约合并与年轮迭代修订）

> 承接 `员工蒸馏器-设计文档-v0.3.md` 与 `员工蒸馏器-从doc2md反推蒸馏路径.md`。
> v0.4 的全部新增都来自对真实沉淀工具 `doc2md.py` 的反推：把抽象的"最小单元/递归合并"换成可对照代码的具体机制。
> 日期：2026-06-20

---

## 0. 这版修了什么

v0.3 把"确定性"收编进了最小单元。v0.4 用 `doc2md.py`（一个 920 行、已人工沉淀的真实工具）当终态样本反推，修正了三处此前一直谈错或谈空的地方，并补上一条缺失的轴。

### 0.1 v0.4 相对 v0.3 的增量

1. **重定义最小单元**（§7）：最小单元 = **一个自洽的可复用目的**，不是最小函数。`doc2md` 是**一个**单元（内部多格式分支），不该被拆成 pdf-to-md / docx-to-md 一堆小工具。撤销 v0.3 之前的过度拆分倾向。
2. **合并主轴改为契约/目的**（§8）：`process_pdf` 与 `process_docx` 几乎零共享代码，绑定它们的是 I/O 契约 + 目的。代码/AST 相似度从主轴降为副轴。
3. **三选一分类 + 迭代轴**（§6、§8、§9）：每个新碎片是 **新工具 / 同目的新分支 / 对已有分支的就地迭代** 三选一；补上 v0.2/v0.3 缺失的**版本/迭代轴 + lineage + 回归**。
4. **价值靠复用确认，存储带伤疤**（§5、§8、§10）：年轮型价值（输出手艺）测不到也 diff 不出，只能靠属性判定 + 持续复用；归一化只用于检测重复，入库必须是带全部边缘处理的原始实现。
5. **warm-start 回灌**（§11）：下次同目的任务先把已有工具捞给宿主，避免重搓——这是蒸馏器价值闭环的一半。

### 0.2 v0.3 保留不变的部分

- 确定性同质作为最小单元判据、Gate 0 确定性探测、四层 Gate（§5）
- MCP 作为统一执行层、宿主适配器控制发现/注入（§4、§11）
- 证据库 / 目录结构 / 指标体系骨架（§12、§14）

### 0.3 v0.4.1 补丁（修复对原始需求的两处漂移）

1. **找回"递归合并"**（§8.5）：原始需求点名要"递归合并"，v0.4 的分支生长只覆盖"目的内累积"，漏了"已转正父能力之间的周期性再聚类/收敛"。补 §8.5。
2. **提示词沉淀标为 deferred 而非砍除**（§2.2）：原始需求要求沉淀提示词及提示词生成的高频代码；MVP 只是延后到阶段三，不是永久不做。

---

## 1. 一句话定义

员工蒸馏器从 agent 的真实执行轨迹中，发现反复手搓的程序类能力碎片，**沿契约/目的把碎片增量长成成熟工具**（如 `doc2md.py` 这种自洽目的、内部多分支、年轮累积的形态），经复现、属性验证、合并、迭代后沉淀为可检索、可回放、可复用、可回灌的能力资产，并通过 MCP 重新提供给后续任务。

---

## 2. 核心边界

### 2.1 纳入范围

- 可执行、单一**目的**、可重放的程序类能力（目的内可多分支，见 §7）
- 有清晰输入输出契约的能力
- 确定性同质的单元（§7.2）

### 2.2 明确不做

- 纯提示词沉淀（**deferred · 阶段三恢复**，非永久砍除——原始需求要求沉淀提示词及由提示词生成的高频代码，因其无二元反馈、需软指标，故 MVP 不做）
- 任意长链 workflow 自动拆 DAG
- 团队共享池、复杂权限治理
- 非确定性单元的自动转正（OCR/识图/LLM/实时网络，先切出、记录）
- 跨宿主完全一致的原生交互体验

### 2.3 MVP 最小定义

接受这样的候选：单一目的、可单独执行、输入输出可保存、可样例重放、确定性同质。其余先只记录。

> 关键澄清（v0.4）：「单一目的」不等于「单一格式/单一函数」。`doc2md`（任意文档→md）是一个合法的单一目的，内部 pdf/docx/xlsx 分支是同一目的的不同输入路径，不应拆开。被排除的是「不同目的拼成的长链」（如 视频→文本→脚本→视频），而不是「一个目的的内部分支」。

---

## 3. 设计原则

1. **程序类优先**：只做有执行反馈的能力。
2. **复现先于评估**：先证明能稳定重放，再谈值不值得入库。
3. **合并靠契约，不靠代码相似**：同目的、同 I/O 契约才是合并依据；代码相似只用于分支内去重。
4. **价值靠复用，不靠跑通**：年轮型价值测不出，靠属性判定 + 持续被复用确认。
5. **存储带伤疤，检测才归一**：入库存原始实现（含所有边缘处理）；归一化仅服务于重复检测。
6. **最小单元 = 自洽目的**：目的内多分支是正常形态；跨目的长链才需拆。
7. **检索宁可漏召回，不要乱召回**。
8. **血缘必须可追溯**：合并、拆分、晋升、降级、就地迭代都要有 lineage。
9. **最小单元即确定性边界**：混合确定性与非确定性是该拆的信号。
10. **先单人闭环，再团队扩展**。
11. **无感是硬约束，不是默认属性**（见 §3.1）。

### 3.1 无感 = 硬性非功能需求 + 4 条守则

原始需求"倾向于无感"必须当成可验收的硬约束来工程化，否则会自然破功。无感靠三进程把"重活"与"你的会话"物理隔开（采集 hook 在 CC 内、蒸馏 daemon 与 MCP server 在带外），并守住 4 条：

1. **采集 hook 极轻且静默**：CC 进程内唯���同步点，只能 append-only 写一行事件即退出（不算 hash/不解析/不联网，<5ms），且不向 stdout/stderr 输出任何字（否则 CC 会显示出噪声）。重活全部丢给 daemon。
2. **daemon 低优先级 + 空闲触发 + 限流**：沙箱重放/装依赖是 CPU/磁盘密集的；必须 `nice` 降权、优先在 CC 空闲窗口批量跑、并发限流——不是候选一出现就立刻重放，而是攒批等空闲。
3. **沙箱隔离在临时目录**：重放只在 temp dir + 拷贝输入里执行，永不在你的项目工作区跑，杜绝动到工作区文件。
4. **warm-start 默认被动**：MCP 只暴露工具让模型自己发现并决定调用，不主动往上下文塞提示；主动提示作为可选开关。

> 验收：无感不是"看起来没打扰"，而是可测的——每工具调用的 hook 延迟、daemon 活跃时的资源占用、沙箱是否触碰工作区，都应有界并被监控（计入 §14 指标）。

---

## 4. 系统分层

四层不变：采集层（每家 agent 一个薄 adapter → 统一 trace）、归档层（证据库：轨迹/快照/样本/环境）、蒸馏层（提取/重放/验证/分类/合并/迭代）、服务层（MCP 统一执行 + 宿主适配器控制发现与注入）。

**MCP 统一的是执行接口，不统一宿主的发现和注入体验。**

---

## 5. 正确性模型

### 5.1 不能用"跑通 = 正确"，也不能用"diff 基线 = 正确"

v0.3 已说明跑通 ≠ 正确。v0.4 补一条同样重要的：**对价值在于"输出比基线更好/不同"的工具，不能拿开源基线当正确性 oracle**。

`doc2md` 的全部价值就是输出**故意**不同于 markitdown（表格拆成列表、阅读顺序重建、键值表特判）。拿 markitdown diff 它，等于惩罚它的价值。

### 5.2 Gate 链（Gate 0 + 四层，Output Gate 按价值类型选 oracle）

#### Gate 0：确定性探测（前置）

探测网络/外部模型/时间随机源 + 多次重放实测，分级为 `deterministic` / `nondeterministic` / `mixed`。`mixed` 退回 §7 拆分；`nondeterministic` 本期切出不转正。（细节同 v0.3）

#### Gate 1：Replay Gate

隔离环境可重放：依赖可装、入口可跑、样例可复现、无未声明环境依赖。通过 = "可运行"。

#### Gate 2：Output Gate（按确定性 + 价值类型选 oracle）

- **有可信真值** → fixture / hash 对比
- **无真值、纯确定性** → label-free 属性 oracle：往返/幂等、结构不变量（标题数/表格数/链接数/图片引用数守恒）、覆盖率（非空、无大段丢字）
- **价值在于差异化输出**（如 doc2md）→ **不与基线做等值比较**，只判属性达标：产出是合法 md、图片引用都落了文件、表格/键值都渲染、无大段内容丢失
- **非确定性**（后续版本）→ 置信度/容差/结构断言

> 开源库的双重角色：`process_generic` 内部就调 markitdown，所以 markitdown 对 generic 分支是「兜底实现 + 空测」，对 pdf/docx 自定义分支才是「有意义的对照基线」。不能一刀切当 oracle。

通过 = "输出大致达标"，但**最终价值由 §5.4 的复用事实确认**。

#### Gate 3：Contract Gate

输入/输出类型可描述、错误条件可记录、无明显项目私有硬编码。通过 = "有跨任务复用机会"，且**产出该能力的契约签名**（合并主轴要用，见 §8）。

#### Gate 4：Promotion Gate

复用频次、跨项目复现、最近成功率、可召回性、是否与现有能力重复/构成分支或迭代。

### 5.3 Gate 工程含义

同 v0.3。新增：Gate 3 必须显式产出**契约签名**（标准化的输入/输出类型 + 目的摘要），它是 §8 合并的 key。

### 5.4 价值确认 = 属性达标 + 持续复用

年轮型价值（输出手艺）无法被 Gate 充分度量。补一条长期信号：**一个能力被跨项目反复召回且成功，本身就是它"对"的最强证据**。Promotion 后仍持续跟踪复用率；长期不被复用的自动降级。

---

## 6. 入库策略

### 6.1 双晋升路径

- **路径 A 统计晋升**：≥3 次复用 + ≥1 次过 Output Gate + 近期成功率达标 + 非近重复垃圾。
- **路径 B 高质量单样本晋升**：明确契约 + 高质量 fixture + 过全部 Gate（Gate 0 判为 deterministic）+ 人工 pin / 系统标高价值。

### 6.2 每个新碎片的三选一分类（v0.4 核心）

拿到一个通过验证的新碎片 C，先按 §8 的契约/目的找到最匹配的现有能力 S，然后三选一：

1. **新工具**：找不到目的/契约匹配 → 建新能力。
2. **同目的新分支**：匹配 S，但 C 处理的输入类型 S 还没有分支 → 作为分支并入 S（如 docx 之于已有 pdf 的 doc2md）。
3. **对已有分支的就地迭代**：匹配 S 且输入类型已有分支 B →
   - C 与 B 结构等价、无新行为 → **重复**，丢弃或仅 +1 复用计数；
   - C 多处理了 B 失败的边缘 / 加了边缘处理 → **迭代**，改进 B、版本 +1、挂回归测试（如 `_repair_docx_relationships` 之于 docx 分支）。

> 判错代价：迭代当新工具 → 工具爆炸；新工具当迭代 → 污染好工具。MVP 阶段第 3 类的"迭代 vs 重复"建议人工 pin 兜底。

### 6.3 状态机

`raw → candidate → verified → promoted → deprecated → archived`，含义同 v0.3。
新增：`promoted` 能力的**分支与版本在内部演进**，不改变顶层状态；每次分支新增/就地迭代写入 §9 的 `branches` / `iteration_log` 与 `lineage`。

---

## 7. 粒度策略

### 7.1 最小单元 = 自洽目的（重定义）

`doc2md` 给的教训：一个值得沉淀的工具是「一个目的 + 内部多输入分支 + 共享后处理 + 年轮迭代」。因此：

- **不拆**：同一目的的不同输入分支（pdf/docx/xlsx 都归 `doc2md`）。
- **要拆**：不同目的拼成的长链（视频→文本→脚本→视频）；确定性异质的混合单元（转换 + 识图）。

### 7.2 第一版最小单元判定

同时满足：单一**目的**、主动作围绕该目的、可脱离原任务重放、输入输出有固定契约、可构造独立验证样本、**确定性同质**。

确定性异质 = 拆分信号（同 v0.3）：doc→md（确定性，图原样另存）与 识图（非确定性）是两个单元。

### 7.3 后续升级图级拆分

阶段二/三再做调用链追踪、中间产物血缘、节点共享度统计、确定性边界自动识别。

---

## 8. 合并策略（v0.4 重写）

### 8.1 合并主轴 = 契约 + 目的；副轴 = 代码相似

`process_pdf` 与 `process_docx` 几乎零共享代码，但属于同一能力。**故主轴必须是契约/目的，代码/AST 相似度只做副轴**，用于分支内判"重复 vs 迭代"和识别"薄壳"。

多阶段流水线据此调整：

| 阶段 | 手段 | 作用 | 主/副 |
|---|---|---|---|
| 契约匹配 | 标准化 I/O 契约签名比对 | 判"是不是同一能力/同一分支" | **主轴** |
| 目的聚类 | 目的摘要 embedding（Qdrant） | 契约模糊时辅助判同目的 | **主轴** |
| 结构相似 | tree-sitter / ast-grep 归一后比对 | 分支内判重复、识别薄壳 | 副轴 |
| 文本/精确 | BLAKE3 / MinHash | 抓完全重复与近重文本 | 副轴 |

> 薄壳识别：若候选只是包了某开源库（如 generic 路径包 markitdown），ast-grep 一看即知，直接判结构重复，连测都省。

### 8.2 合并形态 = 父目的 + 分支 + 共享后处理

不把所有实现揉成一坨，也不是一堆并列小工具，而是**一个能力内部 dispatch**（正是 `convert_file` 的形态）：

```text
能力：doc-to-markdown        # 目的 + 统一契约 + 统一检索入口
  ├─ branch: pdf   (process_pdf)
  ├─ branch: docx  (process_docx)
  ├─ branch: xlsx / pptx / generic(markitdown 兜底)
  └─ shared post-processing: table_to_list, sanitize_text   # 跨分支共享改进，提到父级
```

### 8.3 自动合并/挂靠前提

1. Gate 1 / Gate 2 通过
2. 契约可统一（同目的、I/O 兼容）
3. 确定性等级一致（不把 deterministic 与 nondeterministic 并入同一父能力）
4. 新分支/迭代不破坏已通过的其它分支（跑共享后处理回归）

不满足时只记"近邻候选"，不自动合并。

### 8.4 存储带伤疤，回退可追溯

入库存**原始实现**（含 `_repair_*`、满地 try/except、键值正则）；归一化只用于 §8.1 检测，绝不进存储。每次合并/分支/迭代保存：合并前快照、实现引用、血缘、理由、失败与保留样本。新样本暴露分叉可自动拆回。

### 8.5 递归合并（找回原始需求的"递归"）

分支生长解决"目的内累积"，但"递归合并"还要求两层收敛：

1. **批内递归**（Yunjue 式）：Batch Evolution 里，Composer 产出的结果**再次进入 Aggregator 聚类**，直到一轮内不再产生新的并入——收敛即停。防止"A 并入 B 后，AB 又该并入 C"这种一次没合干净。
2. **父能力间周期性再聚类**：已转正的父能力之间，定期按契约/目的重新聚类——若两个父能力其实是同一目的（例如早期被误判成两个 skill 的 `doc-to-markdown` 与 `office-to-markdown`），触发**父级合并**：一方降为另一方的分支集，年轮全部保留。

护栏（都沿用既有原则，不新增松动）：

- 父级合并走 §8.3 的全部前提 + §6.1 的 shared_promotion 式实测门槛
- 全程 lineage 可追溯、可回退；合并错了能拆回两个父能力
- 收敛信号：一轮再聚类无新合并 → 停止，避免无限震荡

> 一句话：分支生长是"目的内长大"，递归合并是"目的间收敛"——后者是原始需求"递归"二字的落点，频率低、门槛高、必可回退。

---

## 9. 数据结构（v0.4 重构为"目的 + 分支 + 迭代"）

### 9.1 能力 Manifest（渐进式分层）

> 格式约定（借 Letta MemFS）：manifest = YAML frontmatter（`description`/`when_to_use` 等 stub 字段**常驻可见**，渐进披露靠"目录位置 + stub 决定是否加载全文"）+ markdown body；用 **git 版本化**，commit message 结构化（`tool:X | branch:Y | status:done`），git log 即 `iteration_log` 的一部分。
> **关键边界（与 Letta 相反）**：manifest 由 **hook/daemon 写，不由宿主 agent 写**——Letta 让 agent 自己 bash 管记忆（有感），我们只借文件/版本约定、写入走带外管线，守无感（§3.1）。

```yaml
# --- L1 元数据层（召回仅注入这层）---
name: doc-to-markdown
purpose: 任意文档转换为 LLM 友好的 markdown
when_to_use: 需要把 pdf/word/excel/ppt/html 等统一为 markdown 时
contract:
  input: { document_path: file, type: "auto|pdf|docx|xlsx|pptx|generic" }
  output: { markdown: string, assets: dir }
tags: [convert, markdown, document]
determinism: deterministic
quality: { reuse_count: 42, success_rate: 0.95, last_used: "..." }

# --- L2 正文层（展开后加载）---
branches:                       # 同目的的输入分支（T3）
  - key: pdf
    impl_ref: ./process_pdf.py
    fixtures: [...]
  - key: docx
    impl_ref: ./process_docx.py
    fixtures: [...]
  - key: generic
    impl_ref: ./process_generic.py   # 包 markitdown，标记为薄壳兜底
    is_thin_wrapper: true
shared_post_processing:         # 跨分支共享改进（T5），提到父级
  - table_to_list
  - sanitize_text
host_native: { mcp_tool: doc_to_markdown }

# --- L3 资源层（执行/回归时加载）---
shared_fixtures: [...]          # 共享后处理回归集

# --- L4 治理/血缘层（合并/审查时加载）---
iteration_log:                  # 就地迭代轴（T4）
  - { ver: 3, branch: docx, change: "add _repair_docx_relationships", regression: pass, ts: "..." }
  - { ver: 4, scope: shared, change: "add table_to_list", regression: pass, ts: "..." }
lineage: { merged_from: [...], snapshot_refs: [...] }
source: { author, team_visibility: private, origin_host: claude-code }
```

### 9.2 关键变化

- 从"单实现工具"升级为"父目的 + branches + shared_post_processing"，直接对应 `doc2md` 的真实形态。
- `iteration_log` + `lineage` 提供 v0.3 缺失的版本/迭代轴。
- `is_thin_wrapper` 标记薄壳兜底分支，影响 Output Gate oracle 选择。

---

## 10. 静态分析的正确定位

能做（启发式）：硬编码/密钥扫描、副作用 API 粗筛、依赖缺失检查、代码指纹/近重初筛、确定性信号粗筛、**薄壳识别**。

不能宣称：完全确定性/无副作用、真正最小粒度、输出语义正确。

**归一化只用于检测重复，不用于入库存储**（修正③）。确定性以多次重放实测为准，静态信号仅初筛。

---

## 11. 检索、注入与 warm-start 回灌

### 11.1 检索目标与排序

把"最值得尝试且误召回风险最低"的排前面。排序 = 语义相似 + 契约匹配度 + 近期成功率 + 失败惩罚 + 作用域匹配 + 新鲜度。

### 11.2 渐进式注入

默认只注入 L1（名称/目的/适用条件/契约签名/质量分），模型请求后再展开 L2/L3/L4。

### 11.3 warm-start 回灌（reuse-before-create）

蒸馏器价值的另一半：**在 agent 即将手搓某目的代码前，先把已有能力捞出来**。

- 触发：新任务的契约/目的与某 promoted 能力匹配。
- 行为：注入该能力的 L1，让宿主优先调用而非重写（对应 T2：项目B 不该重搓 项目A 的 pdf 转换）。
- 副产物：若现有能力缺该输入分支，agent 的新实现正好成为"同目的新分支"候选，工具继续生长。

### 11.4 MCP 定位

MCP 负责统一调用入口、暴露接口、管理工具描述；宿主适配器负责何时检索、检索几条、如何排序、是否自动注入、是否需权限确认。

---

## 12. 数据存储

```text
distiller/
  traces/       # 统一 trace 事件（含 determinism_signals）
  artifacts/    # 输入输出样本、stdout/stderr、代码快照
  candidates/   # 候选（含 deferred:nondeterministic 切出项）
  skills/       # 已转正能力（父目的 + branches + shared_post_processing）
  indexes/      # 契约签名索引、向量、MinHash、结构模板、代码指纹
  fixtures/     # 分支 fixture + 共享后处理回归集
  lineage/      # 合并/拆分/晋升/降级/迭代历史
  reports/      # 指标与批处理报告
```

运维：`traces/`、`artifacts/` 需保留策略（按时间窗 / 是否已转正 / 是否被 lineage 引用 GC）。

---

## 13. MVP 方案（用 doc2md 当目标终态）

### 13.1 宿主 / 范围 / 定位

> **MVP 定位（经 grill 重定（见 `grill结论与MVP重定位.md`）：** 单人 MVP 不是独立产品，是**团队版的去风险原型**——廉价验证"捕获→蒸馏→塞回工具箱"管线，真正的价值在团队跨人复用。所以：别过度加固，数据模型现在就按团队设计（`created_by`/`visibility`/跨人去重/`requires_host_capability`），把"跑别人代码的信任与沙箱"当真风险。

只做 Claude Code；只做"文档转 markdown"这一**目的**（内部多格式分支）。范围按 grill 第 6 条走 **B**：**只自动转正纯 Python/无系统依赖分支**；系统依赖分支（doc2md 的 LibreOffice/Office COM 老格式分支）由 Gate 0 检测后标 `deferred_dependency` + 填 `requires_host_capability`，**显式搁置不静默失败**。MVP 的检验：**能不能从 trace 把反复手搓的 X→md 片段，增量长成一个 `doc2md` 形态的能力种子（非成品）。**

### 13.2 自动化到这里为止

1. 轨迹采集（CC hooks / transcript）
2. 候选发现（受范围约束：识别"读某类文件 → 产出 .md"的单入口进程）
3. Gate 0 确定性探测
4. 沙箱重放（Replay Gate）
5. 属性 Output Gate（往返/不变量/覆盖率，差异化输出不与基线等值比较）
6. 契约/目的聚类 → 三选一分类（新工具/新分支/迭代）
7. 父目的 + 分支 + 共享后处理的保守合并
8. 检索 + MCP 暴露 + warm-start 回灌

先不自动化：任意长链拆分、非确定性单元转正、团队共享、提示词沉淀、"迭代 vs 重复"的全自动判定（人工 pin 兜底）。

### 13.3 成功标准（经 grill 重定：沉淀的是"种子"非"成品"）

1. 从 trace 中把 ≥1 个目的（doc→md）长成含 ≥3 个**纯 Python 输入分支**的能力**种子**（不期望一次到 doc2md 成品质量）
2. 出现 ≥1 次就地迭代被正确识别（不新建工具，挂回归通过）
3. **测到真实复用**：≥N 次 warm-start 命中 + 直接调用而非重搓——**这才证明库被取用，而不只是被填满**（复用率，而非沉淀量，是真成功信号）
4. 系统依赖分支被**显式标注搁置**（可见，非消失）；诚实声明 doc2md 是"部分尺子"（老格式分支在 MVP 沙箱过不了 Replay Gate）
5. 人工审核不在关键路径上（仅 ambiguous 不可比时兜底）

---

## 14. 指标体系

- `Replay Pass Rate` / `Output Pass Rate`（属性达标率）
- `Promotion Rate`
- `Top-1 / Top-3 Recall`
- `Contract Merge Precision`（按契约合并的正确率，替代旧的代码相似合并）
- `Branch Growth`（一个目的累积的分支数 / 误并率）
- `Iteration Classification Accuracy`（迭代 vs 新分支 vs 新工具判对率）
- `Iteration Regression Pass`（就地迭代不回退既有分支）
- `Warm-start Hit Rate` / `Warm-start Gain`
- `Determinism Split Rate`
- `Review Load`

---

## 15. 路线图

### 阶段一（CC，doc→md，单人）
- CC 采集 + Gate 0 + Replay/Output 属性验证
- 契约/目的聚类 + 三选一分类（迭代判定人工兜底）
- 父目的+分支+共享后处理合并
- MCP 服务 + warm-start 回灌

### 阶段二
- 接 Codex（结构化 JSONL 验证统一 schema）
- 完善 Contract / Promotion Gate，自动化"迭代 vs 重复"
- 启用非确定性单元的属性 oracle（解锁识图等切出单元）

### 阶段三
- 接 Hermes（处理其 learning loop 共存/接管）
- 长链拆分实验（确定性边界自动识别）
- 团队共享池 + 签名/权限/沙箱治理

---

## 16. 待拍板事项

1. `MVP = Claude Code + 文档转 markdown + 单人本地库`
2. `N >= 3` 仅为统计晋升路径之一，非唯一
3. `Replay Gate` 与 `Output Gate` 分离，"跑通"不视作"正确"
4. 第一版不做任意长链自动拆解
5. MCP 只作统一执行层，不作统一宿主体验层
6. "确定性同质"作为最小单元判据，MVP 只自动转正纯确定性单元
7. **最小单元 = 自洽目的**：`doc2md` 算一个能力（内部多分支），不拆成单格式小工具
8. **合并主轴 = 契约/目的**，代码相似度降为副轴
9. **三选一分类 + 迭代轴**：MVP 阶段"迭代 vs 重复"接受人工 pin 兜底
10. **价值靠复用确认、存储带伤疤**：差异化输出不与开源基线等值比较，归一化只用于检测

---

## 17. 一句话结论

v0.4 把 `doc2md.py` 从"一个例子"变成了"设计的尺子"：一个值得沉淀的工具，是**一个自洽目的、内部多分支、由年轮（格式扩展 / 健壮性伤疤 / 输出手艺）累积而成**的东西。蒸馏器的真正本事，是**沿契约/目的把碎片增量长成终态，并原样保住所有年轮**——合并靠契约不靠代码相似，价值靠复用不靠跑通，存储带伤疤不做归一。这三条，加上 warm-start 回灌，构成 MVP 的命门。
