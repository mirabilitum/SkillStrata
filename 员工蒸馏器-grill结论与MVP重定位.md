# 员工蒸馏器 · Grill 结论与 MVP 重定位

> 这份是一次 grill-me 式拷问的产出（grilling skill：逐分支、一次一问、压力测试整套规划）。
> 七条核心结论全部在压力下定下，并据此**重写 MVP 定位**。
> 这是回去修订设计文档（v0.4 / 技术栈 / 运维 / 观测审查 / 契约引擎）的依据。
> 日期：2026-06-20

---

## 1. 七条结论（决策表）

| # | 拷问的点 | 结论 |
|---|---|---|
| 1 | 核心假设是真是空 | **成立**：中频（3-4 次）工具因**摩擦**（打断心流、当场判不出值不值）而丢失，无感捕获正当；价值在团队复利 |
| 2 | "有意识维护共享 skill" 这个简单替代行不行 | **不行**：对长尾是死的（人麻烦、会忘、当场判不出）；doc2md 是例外不是规律 |
| 3 | 目标是稀有长尾还是中频 | **中频**（选 A）：N≥3 有效；真·用一次就忘的稀有长尾**不是**目标 |
| 4 | 复用怎么真发生 / MVP 测什么 | **被动起步、质量门控逐步升级**（证明好了才推得狠，绝不 force-push 烂工具）；复用率终究**要被测量**，不能只测沉淀 |
| 5 | 人审会不会被跳过、库会不会烂 | **质量门控自动升级防烂**（不靠人审承重）；人审退成**纯 pull 的例外兜底**；暂不加"久不审就 push"的信号 |
| 6 | 系统依赖工具重放不了 | **走 B**：MVP 只自动转正纯 Python/无系统依赖；**预留 A 的 `requires_host_capability` 字段** + Gate 0 增加系统二进制检测，命中标 `deferred_dependency` 显式搁置（不静默失败） |
| 7 | 价值是跨人的，MVP 却是单人的 | **MVP = 团队去风险原型，不是单人产品**（见 §2） |

> 顺带澄清（用户提的公平一点）："单人先跑通"和"程序优先、砍提示词"是早先的建议，**依然成立**——前者是**顺序**（最便宜验管线），后者程序有客观二元反馈、跨人可重验（提示词不可验、跨人信任更差，故砍得更对）。Q7 改的是**定位**不是顺序。

---

## 2. 最大产出：MVP 从"单人产品"重定位为"团队去风险原型"

整场拷问反复暴露同一件事：**每次价值变强都靠团队**（Q1 团队池 / Q3 团队频率 / Q6 跨人复用 + 人员迭代），单人那侧用户自己一路承认是弱的。用户的杀手例子就是团队的：

> "我做了 doc2md，另一个人做了 pdf2md，第二天我需要 pdf，正好直接复用。"

因此 MVP 的正确理解：

- **不是**有独立价值的单人产品。
- **是**廉价验证管线机制（捕获→蒸馏→塞回工具箱）的原型，目的为团队版**去风险**。

### 三个直接后果

1. **别过度加固单人版**：Codex 两份审核里大半硬化（ReviewItem schema、visibility 阶梯、Composer 两阶段原子写、SQLite 迁移/备份、多进程健康分项……）对一个去风险原型**过早**，搁置到团队/生产化阶段。
2. **数据模型/契约引擎现在就按团队设计**（设计期几乎免费，事后 retrofit 很贵）：
   - `created_by` / 归属
   - `visibility`（hidden→searchable→exposed→team_published）
   - 跨人去重的 key
   - `requires_host_capability`（第 6 条预留）
3. **团队真难点现在就当最大风险摆上台面**，不是"阶段三治理小事"：
   - 自动跑**别人**蒸馏出的代码 → 信任与沙箱安全（这才是 sandbox 从"隔离"升级为"安全边界"的真正理由）
   - 跨人去重 / 归属 / 人员流动后的能力存续

---

## 3. 对现有文档的影响

| 文档 | 改什么 |
|---|---|
| v0.4 | §13 MVP 定位改写：种子非成品 + 团队去风险原型 + 系统依赖走 B；§9 manifest 加 `requires_host_capability` / `created_by` / `visibility` 预留字段 |
| 技术栈 | Gate 0 增加"系统二进制调用"检测；Replay 明确"纯 Python 自动转正，系统依赖标 deferred_dependency" |
| 观测审查层 | 接受 Codex 审的几个真错（active 不自动替换 / 误合并冻结影响面 / promoted≠exposed / 严重事故可低噪声暴露 / §5.5 引用改 §5.4），其余硬化**搁置** |
| 契约引擎 | behavior_signature 的字段级比较规则（Codex P1，真该补——支配判定要靠它）需补；其余 park |
| 运维层 | processing_stage 别用字符串比较（真 bug，改显式 rank）；credential 改"解析链不假设继承 CC"（我之前写太满）；其余生产化项 park |

> 原则：**真错与真设计漏洞改（约 4-6 条），生产化镀金一律 park 到原型验证之后。**

---

## 4. 新的 MVP 验收标准（替换旧的"沉淀出 doc2md"）

旧标准测的是**捕获**，错了。新标准：

1. **从 trace 沉淀出一个会随复用变好的"种子"**（不是成品 doc2md）。
2. **至少发生 N 次真实复用**（warm-start 命中），证明库被取用，而不只是被填满。
3. **纯 Python 分支跑通全程**；系统依赖分支**被显式标注搁置**（可见，不是消失）。
4. 诚实声明：**doc2md 是"部分尺子"**——它的老格式分支在 MVP 沙箱里过不了 Replay Gate，host-capability 重放是团队阶段的已知硬需求，不假装 uv venv 够用。

---

## 5. 一句话

这场 grill 的净结论：**核心假设成立（中频工具因摩擦丢失，无感捕获正当），但价值真正在团队；所以单人 MVP 是去风险原型而非产品——别镀金、按团队设计数据模型、把"跑别人代码的信任"当真风险。** 系统依赖走 B（纯 Python 自动转正、系统依赖显式搁置、预留 host-capability 字段）。验收从"沉淀成品"改成"沉淀种子 + 测到真实复用"。
