# 员工蒸馏器 · 契约引擎设计

> 钻 v0.4 §8 留下的命门：**怎么从一段 trace 碎片抽出 I/O 契约，怎么判两个碎片"同目的"。**
> 整个合并 / 三选一分类 / warm-start 都架在这上面。
> 参照 Yunjue-Agent（arXiv 2601.18226）的实现层，明确「借哪些」「在哪一刀必须分道」。
> 版本：v2（并入《契约引擎设计修改意见》的五条收紧） · 日期：2026-06-20

---

## 0. v2 改了什么（来自修改意见的五条收紧）

v1 方向对，但有几处会"把好东西看成噪声"。v2 收紧五处，并引入三个术语：

1. **分支主键从 `media_type` 升级为 `branch_identity`**（§3.1、§5）——媒体类型太粗，且粒度本身要行为驱动，别枚举。
2. **契约签名从三件套扩为四件套，新增 `behavior_signature`**（§3.2）——承接输出手艺；只细化"目的内"判定，不参与"是否同目的"。
3. **`R_miss` 作用域收紧**到 branch+contract+version+env，并记失败原因（§1）。
4. **best-of-N 只选默认实现，不删兄弟实现**（§2、§6）——拆 `active_impl` / `retained_impls`，防偷偷 canonicalization。
5. **`shared_promotion` 设硬门槛**（§6）——跨分支增益须在 fixture 上实测，否则不提级。

新术语：`branch_identity`、`behavior_signature`、`shared_promotion`。

---

## 1. 从 Yunjue 借什么，不能照抄什么

Yunjue 是最接近的工程化实现，工具生命周期（create→verify→reuse→absorb→generalize）几乎就是我们要的骨架。但目标有一处根本差异：

| 维度 | Yunjue | 员工蒸馏器 |
|---|---|---|
| 工具来源 | 运行时为单 query 即时合成的一次性原语 | 跨项目真实 trace 沉淀的反复手搓能力 |
| 变体性质 | **噪声**：同一工具多次 rollout，择优即可 | **信号**：新分支（新输入）或年轮迭代（边缘处理） |
| 泛化 = | **去特化**：删 query-specific，重写成 canonical | **保年轮**：原样保留边缘处理，外层加 dispatcher |
| 功能等价判据 | LLM 读描述/代码猜 | **真实观测 I/O 当客观锚点** |

**借架构，换 Merger。** 具体借：

1. **检索→判定→新建 三段式 + `R_miss`**
   Manager 先检索全局库，命中复用，否则才新建；`R_miss` 记失败工具防反复调用坏工具。
   → 我们采用，但 **`R_miss` 不是目的级黑名单**（见 §1.1）。

2. **聚类用 LLM 判功能语义等价，不用纯向量阈值**
   → 采用，但喂 LLM 的是**契约签名 + behavior_signature**，非裸代码（§4）。

3. **Batch Evolution（批量吸收，非即时合并）**
   攒批再统一聚类合并，省锁、更稳，可 best-of-N 为分支择优。
   → 采用，但 **best-of-N 只选"默认激活实现"，绝不删除/覆盖兄弟实现**（§2、§6）。

4. **二元反馈 = 执行 pass/fail**
   → 对应 Replay Gate，traceback 结构化存证据库。

### 1.1 R_miss：分支/版本/环境级失败记忆，不是目的级黑名单

一次失败可能只是坏样本/坏环境/旧版本/旧分支，不该把整个目的拉黑。作用域收紧为：

```text
R_miss key = skill / branch + contract_slice + version + env_fingerprint
```

并记**失败原因类别**（`missing_dependency | output_not_pass | timeout | crash`）。失效规则：

- 分支迭代后失效（版本变）
- 环境变化后失效（env_fingerprint 变）
- 命中不同 contract_slice 时不沿用
- warm-start 推荐前先检查 R_miss 是否仍对当前 `branch_identity + env_fingerprint` 生效；`missing_dependency` 类在依赖补齐后即失效

---

## 2. 必须分道的一刀：Merger → Composer

Yunjue 的 **Merger 把一簇重写成 canonical，删 query-specific 逻辑**。对我们这会把 `doc2md` 的 `_repair_docx_relationships`、键值正则、bbox 阅读顺序——90% 的价值——当杂质清掉。

**Composer = 结构组合器，不是语义重写器：**

| | Yunjue Merger | 我们的 Composer |
|---|---|---|
| 动作 | LLM 重写簇 → 一个新文件 | 把原实现原样挂到 dispatcher 分支下 |
| 对原实现 | 丢弃 | 字节级保留（带全部年轮） |
| best-of-N | 择优 = 覆盖替换 | **择优 = 只切换 `active_impl`，`retained_impls` 全保留、可回退、不跳回归** |
| 输出形态 | 单 canonical 工具 | 父目的 + branches + shared_post_processing |
| 泛化来源 | 删特化 | 外层 dispatch + 跨分支后处理**有门槛地**提级 |

> 一句话：Yunjue 用合并**消除**变体，我们用合并**组织**变体。best-of-N 在我们这里只决定"默认激活谁"，不决定"留谁删谁"。

---

## 3. 契约签名：从 trace 碎片抽什么（四件套）

契约是合并/判同的 key。**观测 I/O 为主锚，代码为佐证，目的为语义，行为为价值指纹**：

### 3.1 三件套 + branch_identity

| 成分 | 主信号（客观） | 佐证 | doc2md 例 |
|---|---|---|---|
| 输入契约 | trace `input_artifacts.media_type` / arity | 代码入口、`SUPPORTED` 集、import 库 | `{file, 单文件\|目录}` |
| 输出契约 | trace `output_artifacts`（类型 + 旁产物） | 写文件动作、返回类型 | `{markdown:str, assets/*.png}` |
| 目的摘要 | 任务 NL + docstring → embedding | 函数名、注释 | "文档 → LLM 友好 markdown" |

**branch_identity（分支主键，取代裸 media_type）** 至少含：

- `input_mode`：`single_file | directory`
- `normalized_input_family`：`pdf_text | pdf_scanned | office_docx | office_legacy | generic`
- `artifact_profile`：是否产 `assets`、是否含图片旁产物
- `determinism`：`deterministic | nondeterministic | mixed`
- `key_flags`：是否需 repair、是否为 wrapper fallback

> **粒度警告（重犯过度拆分的风险）**：`branch_identity` 的细分必须**行为驱动**，不是枚举穷举。例：`pdf_text` 与 `pdf_scanned` 在 doc2md 里是 `process_pdf` 内一个 `_is_scanned` 条件分叉，**默认算同一 branch 的内部分叉**；只有当二者 `behavior_signature` 显著不同（如扫描版退化成纯页图、输出手艺完全两路）时，才升格为两个 branch。`media_type` 只是 branch_identity 的一个组成，不是主键。

### 3.2 第四件：behavior_signature（承接输出手艺）

外部 I/O 看起来一样、内部输出手艺差很多的能力（成熟实现 vs 薄壳兜底），光靠前三件会被判成同类。新增 `behavior_signature`，**来源 = Output Gate 的属性验证结果**：

- `markdown_valid`
- `assets_emitted` / `image_ref_coverage`
- `heading_preservation`
- `table_render_mode`：`pipe_table | kv_list | row_list`
- `key_value_rendering`
- `content_coverage_grade`
- `is_thin_wrapper`

> **放置（关键）**：behavior_signature **只用于"目的内"的细化判定**（重复 / 迭代 / 薄壳兜底），**不参与"是不是同目的"**。同目的判定仍只看契约 + 目的摘要。否则两个 `table_render_mode` 略不同的成熟实现会被错拆成两个 skill——那又是过度拆分。

契约签名规范化后落 `meta.sqlite` 的索引表（向量入 Qdrant/pgvector），作为检索与合并的一等 key。

---

## 4. 目的判同：两段式（召回 + 裁决）

借 Yunjue「先廉价召回，后精确裁决」：

1. **预筛（召回，廉价）**：目的 embedding 余弦近邻（Qdrant）+ 契约类型粗匹配 → 候选集。宁可多召回。
2. **裁决（精度，LLM）**：仅对候选边界，让 LLM 判"功能等价吗"，**输入 = 契约签名 + 目的摘要 + behavior_signature，不是裸代码**。比 Yunjue 稳，因为有客观 I/O + 行为指纹锚定。

裁决输出关系标签：`same_branch` / `same_purpose_new_branch` / `different_purpose`，喂给 §5。

> 注：判"是否同目的"主要靠契约 + 目的；behavior_signature 在此只帮裁决"同目的下这俩是不是该并到同一 branch"。

> 实现参考（写 judge prompt 时）：Yunjue 的 `src/prompts/templates/tool_cluster.md` 与 `step_tool_analyzer.md` 可当**反面模板**——它按"名字+描述"聚类且**反对 dispatch**；我们按**契约+目的**聚类、**dispatch 是合法终态**。照它的结构写，把立场反过来。

---

## 5. 三选一分类：契约比对的直接产物

拿到通过验证的新碎片 C，按 §4 找最匹配能力 S 后：

```
若 无目的/契约匹配:
    → 新工具（建新能力）

若 同目的(S) 且 C.branch_identity 与现有 branches 都不兼容:
    → 同目的新分支（并入 S，新增 branch）           # docx 之于已有 pdf

若 同目的(S) 且 C.branch_identity 兼容某现有 branch B:
    若 C 与 B 结构等价(ast-grep 归一后相同) 且 behavior_signature 相近:
        → 重复（丢弃 / 仅 +1 复用计数）
    若 C 的 behavior_signature 明显不同 / 多处理了 B 失败的边缘:
        → 就地迭代（改进 B，版本+1，挂回归测试）       # _repair_docx 之于 docx 分支
```

要点：

- 判定条件从 `media_type ∉ branches` 升级为 **`branch_identity 与现有 branches 都不兼容`**。
- **同 purpose 下 behavior_signature 明显不同的实现，不得直接判重复**——可能是更成熟的迭代，或该并列保留的分支。
- 结构等价判定用 ast-grep 归一（去变量名/字面量/import 顺序）——归一**只用于此判定，不进存储**（v0.4 修正③）。
- "迭代 vs 重复"是最易判错处；MVP 阶段此处人工 pin 兜底（v0.4 §16 第 9 条）。
- 薄壳识别：C 只是包开源库（generic 包 markitdown）→ ast-grep + `is_thin_wrapper` 即知，标为兜底分支，Output Gate 不拿它当对照基线。

---

## 6. 端到端：契约引擎在管线里的位置

```
新碎片 C（已过 Replay + 属性 Output Gate；后者产出 behavior_signature）
  │
  ├─ 契约抽取（§3）：观测I/O主锚 + 代码佐证 + 目的embedding + branch_identity + behavior_signature
  ├─ 目的判同（§4）：Qdrant预筛 → LLM契约级裁决（带 behavior_signature）→ 关系标签
  ├─ 三选一分类（§5）：新工具 / 新分支 / 迭代|重复
  └─ 入批次缓冲（不即时合并；先查 R_miss 是否仍生效）
        │
        ▼  Batch Evolution（借 Yunjue，定期触发）
  Aggregator：按契约+目的聚类批内候选与现有库
  Composer（替换 Merger）：
     - 原样挂分支（retained_impls 全保留）
     - best-of-N 只切换 active_impl，记 lineage，可回退
     - shared_promotion（见下）
        │
        ▼
  写库（branches + active/retained impls + shared_post_processing + iteration_log + lineage）
  → 检索 + MCP暴露 + warm-start回灌（推荐前查 R_miss）
```

### 6.1 shared_promotion：共享后处理的二次晋升门槛

"把跨分支通用后处理提到父级"不是默认动作，是**有门槛的二次晋升**。`table_to_list` 这种看着共享，未必对所有分支纯增益。提级须同时满足：

1. **被 ≥2 个分支显式受益**，且增益在各分支 fixture 上**实测可量**（不是"看着通用"）
2. 提级前后**共享回归全部通过**
3. **无任何兄弟分支质量回退**（behavior_signature 不退化）
4. 保留**降回 branch 层**的回退能力

端到端在 Composer 后增一步：

```text
shared_candidate evaluation → cross-branch fixture regression → promote or keep local
```

---

## 7. Yunjue 生命周期 → 我们的映射

| Yunjue | 我们 | 关键差异 |
|---|---|---|
| Create | 从 trace 发现碎片 + 契约抽取（四件套） | 来源真实 trace |
| Verify | Replay Gate + 属性 Output Gate（产出 behavior_signature） | 加属性 oracle，差异化输出不 diff 基线 |
| Reuse | warm-start 回灌 + 收紧版 `R_miss` | R_miss 不是目的级黑名单 |
| Absorb | Aggregator 聚类 + **Composer 组织** | Merger→Composer：组织而非消除变体 |
| Generalize | 外层 dispatch + 有门槛 shared_promotion | 保年轮，不删特化 |

---

## 8. 一句话结论

Yunjue 给了验证过的骨架——检索三段式、`R_miss`、LLM 功能聚类、批量吸收、执行级二元反馈，全部直接借。唯一自己来的是 **Merger → Composer**：用组织变体保年轮，而非消除变体求 canonical。让我们更稳的底牌，是**用真实观测 I/O + behavior_signature 当锚**，把"功能等价"从"LLM 读代码猜"降成"看真实输入输出与行为对不对得上"。v2 的五条收紧，本质都是同一个原则：**别在最关键处把好东西看成噪声、把年轮看成冗余、把一次失败看成永久否定。**
