# 员工蒸馏器 · 落地实现规划（MVP）

> 经 grill 重定位后的实现规划：单人 MVP = 团队版去风险原型。
> 目标：廉价验证"捕获→蒸馏→塞回工具箱"管线在 CC + 纯 Python doc→md 上跑通。
> 数据模型从 M0 起就按团队设计；系统依赖走 B（标注搁置）。
> 依据：v0.4 / 契约引擎 / 技术栈与运行时 / 运维层 / 观测审查层 / grill结论与MVP重定位。
> 日期：2026-06-20

---

## 0. 范围（钉死，防 scope creep）

**做**：CC 采集 → 候选发现 → Gate0 → Replay → 属性 Output Gate(出 behavior_signature) → 契约抽取 → 目的判同 → 三选一 → Composer → MCP 回灌 → 观测审查 CLI。仅 **纯 Python、doc→md 目的**。

**不做（原型阶段搁置）**：团队池真实联网共享、跑别人代码的沙箱安全、提示词沉淀、系统依赖自动转正、长链拆 DAG、Codex/Hermes 采集、生产化镀金（schema 迁移/死信队列/多进程健康分项/ReviewItem 全 schema）。

**预留不实现**：`created_by` / `visibility` / 跨人去重 key / `requires_host_capability` 字段从 M0 就进 schema（团队 retrofit 免费），但 MVP 不据此动作。

---

## 1. 里程碑与任务

### M0 · 骨架（地基）
- T0.1 repo + `distiller/` 目录树（traces/artifacts/candidates/skills/meta.sqlite/vectors/reports）
- T0.2 SQLite schema（candidates 表含 `stage_rank` 显式编号、lifecycle；skills/branches/lineage/usage_events/quality_rollup/judge_cache/R_miss；**预留团队字段**）
- T0.3 config 加载（解析链：flag>env>config>默认；judge 凭证解析链含 offline 兜底）
- **交付**：空库可初始化，`distiller init` 跑通

### M1 · 采集（无感入口）
- T1.1 CC `PostToolUse` hook：极轻静默 <5ms，append `RawToolEvent`（只原始信息，不解析/不 hash）
- T1.2 早期 artifact snapshot（`Stop` hook 或 daemon early pass，趁工作区文件还在就拷进 artifacts/）
- T1.3 daemon enrichment：RawToolEvent → EnrichedToolEvent（补 hash/media_type/file_changes/determinism_signals）
- **交付**：在 CC 里跑脚本，traces/ 出现事件，前台零打断；Hook Latency p99<5ms 可测

### M2 · 候选发现 + Gate0
- T2.1 事件关联（task_id+cwd+时序 拼成候选记录）
- T2.2 形状过滤（单入口·文档类型进·md 出·成功）+ 噪声预过滤（砍胶水/REPL/调已有 skill）
- T2.3 Gate0 确定性探测 + **系统二进制检测**（soffice/win32com → `deferred_dependency`+`requires_host_capability`，不静默失败）
- **交付**：从 traces 提名 candidate(raw)，纯 Python 进流水线、系统依赖显式搁置

### M3 · Replay + 属性 Output Gate（验证 + 指纹）
- T3.1 uv venv 沙箱（temp 隔离 + 拷贝输入，绝不碰工作区）+ Replay Gate
- T3.2 属性 Output Gate：mistune 解析 + 内在合法性 + **守恒 oracle（独立抽取器测输入，防循环）**，门禁按 `input_profile` 分支化
- T3.3 behavior_signature 生成 + **字段级比较规则**（ordinal/numeric/categorical-no-order）
- **交付**：候选过 Replay+Output，产出 behavior_signature；Sandbox Workspace Touch=0 可测

### M4 · 契约抽取 + 目的判同
- T4.1 契约签名（观测 I/O 主锚 + 代码佐证 + 目的 embed + branch_identity）落 meta.sqlite + Qdrant
- T4.2 目的判同：Qdrant 预筛 → haiku judge 裁决（带 behavior_signature；judge 缓存键含 prompt/schema/model 版本）
- **交付**：新碎片得到关系标签 same_branch/new_branch/different_purpose

### M5 · 三选一 + Composer
- T5.1 三选一分类（偏序：R_miss 修复=iteration 但需过 Output Gate+无回退；B 支配 C 留 fallback 不丢）
- T5.2 Composer：挂分支(retained 全留)/best-of-N 切 active(不删兄弟)/lineage
- T5.3 shared_promotion（结构资格 + ≥2 分支 fixture 实测增益 + 0 回退含容差）
- **交付**：doc→md 种子长出 ≥3 纯 Python 分支 + ≥1 次迭代正确识别 + 回归

### M6 · MCP server + warm-start
- T6.1 MCP 基础工具（search/describe/run_capability）+ `visibility` 阶梯（promoted≠exposed）
- T6.2 一等工具二次门槛导出（高置信才 mcp_exposed）+ warm-start 被动 + R_miss 检查
- **交付**：CC 连上 MCP，能 search/run；**测到 ≥N 次真实复用**（成功标准核心）

### M7 · 观测审查 CLI（pull）
- T7.1 `distiller ls/show/usage/failures`（看库 + 看效果）
- T7.2 `distiller pending`（ambiguous 等待审，默认 stable_active 不替换）+ `pin/resolve/deprecate`
- T7.3 质量门控自动升级（不靠人审防烂）；严重事故低噪声 health
- **交付**：人可 pull 查库/效果/纠偏，库不靠人审也不烂

---

## 2. 依赖与顺序

```
M0 ──> M1 ──> M2 ──> M3 ──> M4 ──> M5 ──> M6
                              └──────────────> M7（可与 M6 并行）
```

主脊串行（每环吃上一环产物）；M7 观测审查只读下游状态，可与 M6 并行。

## 3. 关键验收（对齐 grill 重定的成功标准）

1. 种子非成品：≥3 纯 Python 分支的 doc→md 种子
2. **测到真实复用**（≥N 次 warm-start 命中 + 直接调用）——真成功信号
3. 系统依赖分支显式搁置可见；诚实声明 doc2md 部分尺子
4. 无感硬线：Hook p99<5ms / Workspace Touch=0 / 前台打断=0
5. 团队字段已在 schema（零 retrofit 验证）

## 4. 任务审核

每个里程碑产出（设计/代码/测试）交 **Codex CLI 审核**，审"对不对、漏没漏、有没有违反 grill 结论(别镀金/按团队设计/种子非成品)"，回灌修订后再进下一里程碑。
