# 员工蒸馏器 · 从 doc2md.py 反推蒸馏路径

> 以一个真实沉淀工具 `D:\interact\doc2md.py` 为"终态样本"，反推蒸馏器从碎片到成熟工具该识别、保留、合并、迭代什么。
> 这份文档是后续 MVP 的对齐基准：抽象的"最小单元 / 递归合并"在这里被替换成可对照代码的具体机制。
> 日期：2026-06-20

---

## 0. 为什么用 doc2md.py 反推

之前几版一直在抽象层谈"最小单元""递归合并"，容易跑偏。`doc2md.py` 是一个**人工已经沉淀好的真实工具**（约 920 行，覆盖 pdf/docx/xlsx/pptx/老格式/html/txt，图片原位、表格 LLM 友好化、老格式兜底）。

把它当**终态**，蒸馏器的真正难题就清楚了：**不是"识别一个工具"，而是"沿着年轮把碎片增量长成这个终态"。**

---

## 1. doc2md.py 的年轮（它是一层层长出来的）

| 年轮类型 | 代码证据 | 经历过什么 |
|---|---|---|
| 格式覆盖增长 | `convert_file` 按后缀 dispatch → `process_pdf/docx/xlsx/pptx`，其余落 `process_generic` | 同一目的陆续撞上新输入类型，每次加一个分支 |
| 老格式兜底 | `convert_legacy_format`：先 Word COM，失败再 LibreOffice | 撞上 .doc/.xls/.ppt 才补，COM 挂过才加 LibreOffice 后备 |
| 健壮性伤疤 | `_repair_docx_relationships`、`OpenAndRepair=True`、`CorruptLoad=1`、满地 `except Exception: pass` | 每条对应一次"真实文件炸了" |
| 极具体踩坑 | `_convert_via_word_com_with_timeout` 注释明写为规避 importlib 加载下的 Windows spawn 问题 | 被一个非常具体的 bug 逼出的子进程隔离 |
| 输出手艺 | `table_to_list`、`_is_key_value_table`、手调中文字段正则、`process_pdf` 按 bbox 重排、`sanitize_text` | 不是"能转"，是"转得让 LLM 好读" |

**核心结论：doc2md.py 的价值 90% 在年轮里，不在主干。** 主干（读文件→吐文本）markitdown 就能干（generic 路径正是包了 markitdown）；值钱的是一次次踩坑/调优沉淀下来的边缘处理。

---

## 2. 反推时间线：从碎片到 doc2md，蒸馏器每步该干什么

| 时刻 | 发生了什么 | 蒸馏器必须做的判断 |
|---|---|---|
| T1 项目A | 手搓 10 行 `fitz.open().get_text()` 转 md | 记为候选，频次=1，不转正 |
| T2 项目B | 又手搓一个差不多的 pdf 转换 | 识别"同一目的" → 频次≥2 该转正；**且本该在 T2 把 T1 版本捞给 agent，省得重搓**（warm-start） |
| T3 项目C | 需要 docx，写 `process_docx` | 判定"同目的的新输入分支"，不是新工具 → 并入 doc2md |
| T4 项目D | docx 关系损坏，加 `_repair_docx_relationships` | 判定"对已有 docx 分支的就地迭代" → 更新实现 + 血缘记一笔 |
| T5 项目E | LLM 嫌表难读，加 `table_to_list` | 判定"跨所有分支共享的后处理改进" → 提升到父级，作用全部格式 |

终态 `convert_file` 的 dispatch 结构，就是 T1–T5 这些分支与改进的累积物。

---

## 3. 这条时间线逼出的设计修正

### 修正① 合并的钥匙是"契约/目的"，不是"代码相似度"

doc2md 最尖锐的一刀：`process_pdf` 与 `process_docx` **几乎不共享代码**（一个啃 fitz 的 block，一个啃 docx 的 XML 命名空间）。任何以 AST/结构相似为主轴的合并，**永远不会把它们判为该合并**。

真正绑定它们的是：**输入输出契约一致（任意文档路径 → LLM 友好 markdown 字符串）+ 目的一致**。

→ 合并**主轴 = I/O 契约 + 目的聚类**；代码/AST 相似度退为**副轴**，只用于抓"同一分支的重复实现"（如 T1、T2 两个雷同 pdf dump）。之前几版把主副轴搞反了。

### 修正② 每个新碎片是三选一，不是二选一

不是"重复 vs 新工具"，是 **新工具 / 同目的新分支 / 对已有分支的就地迭代** 三选一。

- 把迭代当新工具 → 工具爆炸
- 把新工具当迭代 → 污染好工具

→ 需要之前漏掉的**版本/迭代轴 + lineage**；MVP 阶段这一步大概率需要人工 `pin` 兜底。

### 修正③ 去重用的"规范化"绝不能动要存的东西

归一化（去变量名/字面量）**只能用于检测重复**。真正**入库存下来的，必须是带全部伤疤的原始实现**（`_repair_*`、满地 try/except、键值正则）。一旦把"简化版"入库，doc2md 的价值就被洗掉了。**检测与存储严格分离。**

---

## 4. 案例：输出手艺——蒸馏器必须识别并原样保留的那类价值

doc2md 的"输出手艺"是"年轮型价值"的最佳样本：靠迭代一层层叠出来、"能不能跑"完全测不到、diff 开源库还会误判为"错"（因为它故意不一样）。

### 4.1 五样手艺各是什么

1. **表格 → 自描述列表**（`table_to_list` / `_render_table`）
   宽管道表拆成 `<!-- row N -->` + `- **表头**：值`。因为宽表列一多，LLM 难把单元格和远处表头对上；拆开后每个数据点自带语义。**为 LLM 读取优化，人看反而啰嗦。**

2. **键值表专门识别**（`_is_key_value_table` / `_is_key_value_label` / `_KEY_VALUE_LABEL_RE`）
   中文表单/简历是"标签|值|标签|值"偶数列结构，按普通表把第一行当表头就全错。探测"偶数列像标签且 ≥50% 行符合" → 改成列两两配对，并去重合并单元格。**最深的一道领域年轮，换领域就得重调正则。**

3. **PDF 阅读顺序重建 + 图文原位**（`process_pdf`）
   不直接 `get_text()`（会乱序），而是收集文字块/图片块的 y 坐标 `bbox[1]` **按 y0 排序**还原阅读顺序，图片插在视觉所在位置；区分内嵌图（type 1，有 xref）与浮动图（`get_images`+`get_image_rects`），`seen_xrefs` 防重。**保住版面语义。**

4. **扫描件优雅降级**（`_is_scanned`）
   前 5 页平均文字 < 100 字判扫描件 → 每页转图输出，**故意不 OCR**，留给下游。正是"识图是另一个单元"在工具边界上的体现。

5. **Unicode 收口**（`sanitize_text`）
   NFKC + 去控制字符，防真实文档的全半角混乱/乱码污染 LLM 输入。

### 4.2 对蒸馏器的三条硬约束

- **价值对朴素指标隐形**：这五样靠"能跑"和"像不像 markitdown"都测不到 → Output Gate 只能**属性判定 + "持续被复用"这一事实**来确认价值。
- **必须原样保留**：归一化/简化会洗掉它 → 印证修正③。
- **它是迭代轴的主要载体**：手艺多以 T4/T5 式"就地改进"叠加，且常是跨分支共享的后处理 → 蒸馏器要能识别"共享后处理"并提升到父级（对应修正②的迭代类）。

---

## 5. doc2md 专门暴露的难点

1. **价值隐形**：见 §4.2，"能跑"和"diff 基线"都测不出真正价值。
2. **测试语料随工具生长**：加 docx 分支时不想重测 pdf，但要保证不破坏共享后处理 → 每分支独立 fixture + 一套共享后处理 fixture，语料随年轮一起长。
3. **就地迭代的回归**：T4 加 repair、T5 加 table_to_list 都要保证不回退已通过分支 → 迭代轴必须挂回归测试。
4. **开源库的双重角色**：`process_generic` 包了 markitdown → markitdown 对 generic 路径是"兜底实现+空测"，对 pdf/docx 自定义路径才是"有意义的对照基线"。不能一刀切当 oracle。

---

## 6. 由此校准的蒸馏器能力清单（替代抽象的"最小单元/合并"）

| 能力 | 具体要求 | 对应年轮 |
|---|---|---|
| 跨项目复现识别 | 频次 + 同分支去重 | T1→T2 |
| 契约/目的聚类（合并主轴） | 按 I/O 契约 + 目的判"同目的新分支" | T3 |
| 三选一分类 | 新工具 / 新分支 / 就地迭代 | T3/T4/T5 |
| 迭代轴 + lineage | 就地改进可追溯、可回归、可回退 | T4 |
| 共享后处理提升 | 跨分支通用改进提到父级 | T5 |
| 价值确认 | 属性判定 + 复用事实，非"能跑"或"diff 基线" | §4 |
| 原样存储 | 带伤疤入库，归一化只用于检测 | 修正③ |
| warm-start 回灌 | 下次同目的任务先捞已有工具，避免重搓 | T2 |

---

## 7. 一句话收束

`doc2md.py` 证明：一个值得沉淀的工具，是**一个自洽目的、内部多分支、由年轮（格式扩展 / 健壮性伤疤 / 输出手艺）累积而成**的东西。蒸馏器的真正本事，不是"认出一个工具"，而是 **沿契约/目的把碎片增量长成终态，并在这个过程里原样保住所有年轮**。合并靠契约不靠代码相似，价值靠复用不靠跑通，存储带伤疤不做归一——这三条是后续 MVP 的命门。
