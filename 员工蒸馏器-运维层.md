# 员工蒸馏器 · 运维层（Operational Layer）

> 之前的文档把"管线逻辑"写厚了，运维层偏薄。这份补齐让三进程真能跑稳、跑省、低摩擦的部分：
> 断点续接 / LLM 凭证与档位 / 配置 / 故障降级 / 密钥与沙箱安全 / 可观测性。
> 配套 `技术栈与运行时-canonical.md`（三进程）与 v0.4 §3.1（无感硬约束）。
> 日期：2026-06-20

---

## 1. 进程独立性：能坏，但不能一起坏

三进程**故障独立**，且有明确的依赖底线：

| 进程 | 挂了会怎样 | 底线 |
|---|---|---|
| 采集 hook | **不能挂**——但它极轻(append 一行)，几乎不会 | hook 失败也必须**静默退出**，绝不报错给 CC、绝不阻塞工具调用 |
| 蒸馏 daemon | 沉淀暂停，trace 继续堆积 | 重启后**断点续接**把堆积的 trace 补上（见 §2） |
| MCP server | warm-start 不可用，CC 看不到沉淀工具 | 不影响你干活，也不影响采集 |

**第一原则：采集是唯一必须永远工作的环，而它正好最轻。** daemon / MCP 都可以离线一段时间，回来补课，期间你无感知。

---

## 2. 断点续接（daemon 故障恢复）

daemon 是空闲触发的批处理，跑一半挂了必须能从中间接。三件套保证：

### 2.1 状态即检查点

每个候选除了生命周期状态（`raw→candidate→verified→promoted`，v0.4 §6.3），再带一个**处理阶段** `processing_stage`，持久化在 SQLite：

```
discovered → gate0_done → replayed → output_gated
→ contract_extracted → judged → classified → composed
```

每个阶段**完成时原子更新** `processing_stage`。daemon 重启：

```sql
SELECT * FROM candidates
WHERE stage_rank < 80 AND lifecycle NOT IN ('archived','deprecated')
-- stage_rank 是显式编号（discovered=10 … composed=80）；processing_stage 是业务阶段顺序，
-- 绝不能用字符串字典序比较（'classified' < 'composed' 这种字典序≠管线顺序，会捞错/漏处理）
```

逐个**从它上次完成的阶段接着跑**。

### 2.2 阶段幂等 + 输入不可变

每个阶段读的是 `artifacts/` 里的**快照副本**（不可变），输出 + 推进阶段。重跑一个已完成的阶段**结果一样**（或直接跳过 `stage < 当前`）。所以"从中间接"安全。

### 2.3 写库原子化

Composer 写 `skills/` 用 **git commit（原子）或 SQLite 事务**：批处理中途死，要么整批已提交、要么没提交可重跑，**绝不留半个坏分支**。

### 2.4 崩溃场景对照

| 崩在哪 | 后果 | 恢复 |
|---|---|---|
| Replay 中（uv venv 内） | venv 是 temp，丢弃 | 停在 `gate0_done`，重启重放（幂等） |
| Judge 中（LLM 调用） | 结果未落盘 | 停在 `contract_extracted`，重判——**命中 judge 缓存（§3.4）则免费** |
| Compose 中 | git commit 原子 | 已提交→标 `composed`；未提交→重 compose |
| 整机断电 | trace 已落盘、状态已持久化 | 重启全量扫描续接 |

> 和我自己 Workflow 的 resume-from-runId 同套路：**不可变输入 + 状态检查点 + 幂等重放 + 原子写**。这是 daemon 的硬性质，不靠运气。

---

## 3. LLM judge：复用 CC 凭证 + haiku 默认

### 3.1 不引入新 provider

judge 跑在 daemon，调 LLM**复用你已有的 Anthropic 凭证**（CC 在用的那把 key），默认模型 **haiku**——就像 subagent 降级到 haiku 一样。**不需要 API 暴露检测、不需要 GPT-5.3/OpenAI、不需要新账号。**

### 3.2 三档，同一把 key

| 档 | 模型 | 用在 |
|---|---|---|
| 默认 | **Claude Haiku** | 绝大多数 judge（结构化分类，有客观 I/O 锚，不吃推理） |
| 升级 | **Claude Sonnet** | 仅 §三选一里 `ambiguous`（不可比）的疑难边界 |
| 可选跨家 | GPT-5.3 | 想要二次意见时才挂，非必需 |

### 3.3 daemon 怎么拿到 key

daemon 是独立进程，不像 subagent 自动继承会话凭证。**凭证解析链**（不假设一定能继承 CC）：

```
1. CLI flag / 显式 config
2. env: ANTHROPIC_API_KEY
3. provider 本地 secret store
4. 都没有 → offline/disabled judge 模式（judge 走人审兜底，不崩）
```

> 注意：**CC 的登录态/订阅态 ≠ 一把可复用的 API key**——很多用户走 OAuth/订阅，env 里并没有 `ANTHROPIC_API_KEY`。所以"复用 CC 凭证"是**支持路径之一，不能写死成一定成立**。key 永不进 trace/artifact/report/git；日志只记 provider + key 后 4 位或 hash 前缀。

### 3.4 省钱与抗抖

- **judge 缓存**：结果按 `(contract_sig_C, contract_sig_S)` 键缓存进 SQLite。重判 / 同对再判**命中缓存=免费**，也是 §2.4 恢复免费的来源。
- **批处理**：judge 随 Batch Evolution 攒批调，不一来一个。
- **API 抖动**：judge 失败 → 候选停在 `contract_extracted` 排队重试，**不阻塞采集和重放**，graceful 退化。

---

## 4. 配置：能改，但默认零配置

### 4.1 默认零配置

key **默认继承现有 `ANTHROPIC_API_KEY`**、模型默认 haiku——**啥都不改就能跑**，合无感/低摩擦。改 config 只为覆盖默认。

### 4.2 优先级

`CLI flag > 环境变量 > 配置文件 > 内置默认`

### 4.3 配置样例（`distiller/config.toml` 或 `.env`）

```toml
[judge]
provider       = "anthropic"            # 默认
model          = "claude-haiku-4-5"     # 默认便宜档
escalate_model = "claude-sonnet-4-6"    # ambiguous 才用
# api_key 不填则继承 ANTHROPIC_API_KEY

[daemon]
idle_trigger        = true
batch_size          = 16                # 参考 Yunjue WORKER 批参数
enhance_interval    = "30m"
max_concurrent_venv = 4
nice                = 10                 # 低优先级（守则②）

[sandbox]
runtime = "uv"                          # MVP；docker 留阶段二系统依赖
workdir = "/tmp/distiller"
network = "off"                         # 重放默认断网（安全+确定性）

[output_gate]
input_extractor   = "markitdown"        # 独立于候选的抽取器（守恒 oracle，补 G1）
text_coverage_min = 0.9

[promotion]
reuse_frequency_N = 3

[storage]
retention_days = 30                     # traces/artifacts GC
```

---

## 5. 故障与降级（每个组件单独失败的处理）

| 故障 | 处理 | 是否影响无感 |
|---|---|---|
| daemon 离线 | trace 堆积，回来断点续接 | 否（采集照常） |
| MCP server 离线 | warm-start 不可用 | 否（不弹错） |
| judge API 抖动 | 候选排队重试 | 否 |
| 沙箱装依赖失败 | 候选标 `deferred`，稍后重试 | 否 |
| 磁盘满 | 触发 retention GC，告警 | 否（采集仍可写，必要时丢最旧 trace） |

**贯穿原则：任何下游故障都不能反向打断采集和你的会话。**

---

## 6. 密钥与沙箱安全

- **key 来自 env，绝不进 traces/artifacts/提交的 config**。
- **采集时脱敏**：hook 抓的 command argv 可能含密钥（`--token=xxx`）→ 落盘前按已知密钥模式 redact。这也和 Contract Gate"无硬编码密钥"呼应，但要在**采集即脱敏**，不等到下游。
- **沙箱是安全边界，不只是隔离**：重放 = 执行抓到的代码。MVP 是你自己生成的代码（风险低），但沙箱仍应 **uv venv + temp dir + 默认断网 + 不挂载工作区**。**团队阶段（跑别人的代码）这条变成强制项**，否则就是供应链风险。

---

## 7. 可观测性

两类指标，写进 `reports/` + 一个 heartbeat 状态文件：

**无感验收（v0.4 §3.1 / 技术栈 §8，硬线）：**
`Hook Latency p99 <5ms` · `Sandbox Workspace Touch =0` · `Daemon CPU@active 有上限` · `Foreground Interruptions =0`

**运维健康：**
`队列深度`（待处理候选）· `judge 调用数/成本`（缓存命中率）· `Replay 失败率` · `断点续接次数` · `磁盘占用 vs 保留策略`

daemon 写 heartbeat（最后活跃时间 + 当前阶段），便于判断它是空闲、在跑、还是挂了。

---

## 8. 一句话

运维层的三条骨：**断点续接**（状态检查点 + 幂等 + 原子写，挂了能续、续了免费）、**judge 复用 CC 凭证 + haiku 默认**（不引新 provider、不需配置就能跑、疑难才升 sonnet）、**配置零默认**（继承现有 key，改 config 只为覆盖）。贯穿一条铁律：**下游可以坏，采集不能停，且坏不反噬你的会话。**
