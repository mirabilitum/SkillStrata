# 员工蒸馏器 · 属性 Output Gate 与 behavior_signature

> 钻主脊里定义最空、下游依赖最多的一环：在**没有标准答案**的前提下，怎么判"输出达标"，并产出喂给契约引擎的 `behavior_signature`。
> 展开 `技术栈与运行时-canonical.md` §3.2 的"属性 Output Gate"行 + `契约引擎设计` §3.2。
> 日期：2026-06-20

---

## 0. 定位：一次解析，两个产物

Replay Gate 只证明"能跑"。属性 Output Gate 干两件事，**共用一次解析**：

| Job | 产物 | 下游 |
|---|---|---|
| **A · 门禁** | pass/fail（最低可接受性） | 候选能不能继续 |
| **B · 指纹** | `behavior_signature`（输出手艺的结构化描述） | 契约引擎判"目的内"重复/迭代/薄壳 |

关键：A 和 B **不是两套系统**，是同一次"解析输出 + 重测输入"喂出的两个结果。哪怕 A 判 fail，B 的指纹照样能产（弱输出也有指纹）。

---

## 1. 真值问题：为什么不能用"对比基线"

我们手里**没有"正确的 markdown"**：
- 工具产出的输出**未经验证**（agent 当时往下走了，没人确认对错）。
- 拿开源基线（markitdown）diff？**不行**——`doc2md` 的价值就是输出**故意**不同于基线（表格拆列表、阅读顺序重建、键值表特判）。拿基线当正确性标准 = 惩罚它的价值。

所以 oracle 必须是**不依赖标准答案的属性**。三类：

| 类 | 看什么 | 角色 |
|---|---|---|
| **内在合法性** | 输出自身 | 硬门禁 |
| **输入↔输出守恒** | 输入与输出的关系 | **核心 oracle** |
| **幂等/重放稳定** | 同输入多次 | 多为确定性检查（与 Gate0 重叠） |

---

## 2. 守恒 oracle（核心）：用输入自己当真值

**洞察：输入文档本身就是"内容该不该活下来"的真值。** 不需要参考答案——重新测量输入里**可量化的量**，检查它们有没有活进输出：

| 守恒量 | 输入侧测量 | 输出侧测量 | 判什么 |
|---|---|---|---|
| 文本覆盖 | 输入可抽文本长度 | 输出去标记后文本长度 | 比值低 = 大段丢字 |
| 图片 | 输入图片数 | `![]()` 引用数 + 落盘 assets 数 | 丢图 |
| 表格数据 | 输入表格的单元格数据 | 输出里这些数据是否出现 | 丢表（**按数据，不按格式**） |
| 结构 | 输入标题/页/sheet 数 | 输出标题/分节标记数 | 结构塌陷 |

### 2.1 两条必须钉死的纪律

1. **用独立抽取器测输入，不能用候选自己。**
   若用候选自己的代码去测输入，它丢了一张图、再测一次也丢——会得出"守恒"的假象（循环）。**输入侧测量必须用一个独立工具**（如 markitdown / 裸库 pdfminer 抽原始文本与图数），与候选实现解耦。

2. **守恒看"内容/数据存活"，不看"格式匹配"。**
   `doc2md` 故意把管道表拆成 `键：值` 列表——所以"表格守恒"要查**每个单元格数据是否出现在输出某处**，而不是"输出里有没有 markdown 表格语法"。这正好和"价值=差异化输出"对齐：我们查内容活没活下来，不查它长得像不像表。

> 守恒 oracle 还有个红利：它**不依赖字节稳定**，所以**对将来非确定性单元（OCR/识图）同样适用**——那时没法 hash 对比，但"图里的字有没有进输出"这种守恒检查照样能用。

---

## 3. behavior_signature：从测量里长出来

Job B 的指纹**大部分是 §2 测量的副产物**——同一次解析直接生成（字段来自契约引擎修改意见 §3.2）：

| 字段 | 怎么来 |
|---|---|
| `markdown_valid` | mistune 能否无错解析 + 无悬空引用 |
| `assets_emitted` / `image_ref_coverage` | 落盘 assets 数 + 引用解析率 + 对输入图的覆盖率 |
| `heading_preservation` | 输入标题 → 输出标题的保留率 |
| `table_render_mode` | 模式匹配输出：`pipe_table \| kv_list \| row_list` |
| `key_value_rendering` | 是否对键值表做了特判（输出现 `键：值` 列表模式） |
| `content_coverage_grade` | 文本覆盖比值分桶（A/B/C…） |
| `is_thin_wrapper` | 见 §4 |

> 这就是为什么"门禁"和"指纹"是一次过：守恒/合法性的测量值，既判 pass/fail，又直接填进指纹。

---

## 4. is_thin_wrapper：基线对比只用于"检测"，不用于"正确性"

这解掉我早先提的"循环陷阱"：
- **检测薄壳**：把候选输出和基线（markitdown）输出比——**几乎一样 = 薄壳**（没年轮）。这是合法的，因为问的是"它是不是就等于那个库"。
- **判正确性**：**禁止**拿基线比——因为成熟工具就该不一样。

所以同一个 markitdown，**当"是不是薄壳"的探针可以，当"对不对"的标尺不行**。两个用途，一条线划开。代码侧再加 ast-grep（候选是不是只 `import markitdown` 无自定义逻辑）双保险。

---

## 5. 门禁阈值（硬 vs 软）

| 检查 | 硬/软 | 说明 |
|---|---|---|
| `markdown_valid`（可解析、无悬空图引用） | **硬** | 不过直接 fail |
| 文本覆盖比 ≥ 阈值 | **硬（但按 `input_profile` 分支化）** | 全局 0.9 会误杀扫描版/表格型/图片型；按 profile：`pdf_text`→文本覆盖硬，`pdf_scanned`→页图引用覆盖硬，`xlsx_table`→单元格数据覆盖硬，`pptx_mixed`→slide 数+资源覆盖组合 |
| 图片/表格数据守恒 | **软** | 记进指纹、降 grade，不直接 fail（工具可能有意丢装饰图） |
| 确定性（多次重放稳定） | Gate0 已判 | 此处不重复 |

> 门禁宁可保守：硬项只卡"合法 + 不大段丢内容"这种谁都不能违反的底线；细粒度差异交给指纹和下游复用去裁。

---

## 6. 一次过的流水线

```
输入：候选重放出的 (input_doc, output_md, emitted_assets)
1. mistune 解析 output_md → AST
2. 内在合法性：可解析? 悬空图引用? 控制字符垃圾?         → markdown_valid（硬门禁）
3. 用【独立抽取器】重测 input：文本长/图数/表数据/标题/页数  → 源测量（真值来自输入本身）
4. 测 output：文本长/图引用+落盘/表数据出现/标题/分节标记   → 输出测量
5. 守恒比值 → content_coverage_grade / image_ref_coverage / heading_preservation
6. 模式匹配 → table_render_mode、key_value_rendering
7. 可选基线对比 → is_thin_wrapper（仅检测）
8. 产出：pass/fail（Job A） + behavior_signature（Job B）
```

---

## 7. 与契约引擎的接口（守住放置纪律）

`behavior_signature` 喂给契约引擎，但**只参与"目的内"判定**（契约引擎 §3.2 已钉死）：

- **不**参与"是不是同目的"——那只看契约 + 目的。
- **参与**"同目的下这俩是不是该并到同一 branch / 是迭代还是重复 / 是不是薄壳兜底"：
  - 同契约、`table_render_mode` 不同 → 不判重复（可能是更成熟的迭代或并列分支）
  - `is_thin_wrapper=true` → 标为兜底分支，且 Output Gate 此后不拿它当对照基线

---

## 8. doc2md 走一遍

1. 重放 `process_pdf` 出 `a.md` + `assets/`。
2. 内在：mistune 解析通过、`![](assets/..)` 全部落盘 → `markdown_valid=true`（过硬门禁）。
3. 用 pdfminer（**独立于 fitz**）重测 `a.pdf`：文本 8000 字、3 张图、2 张表、5 个标题。
4. 测 `a.md`：文本 7800 字、3 引用+3 落盘、2 张表数据都在、5 标题。
5. 守恒：覆盖 0.975 → `content_coverage_grade=A`；`image_ref_coverage=1.0`；`heading_preservation=1.0`。
6. 模式：表格是 `键：值` 列表 → `table_render_mode=kv_list`、`key_value_rendering=true`。
7. 基线对比：与 markitdown 差异大（它会把图丢末尾、表留管道）→ `is_thin_wrapper=false`。
8. 产出：**pass** + 一份 behavior_signature——`process_generic`（包 markitdown）那个分支跑同套会得 `is_thin_wrapper=true`，于是被正确区分为兜底分支。

---

## 9. 一句话

属性 Output Gate 的钥匙是**用输入自己当真值**：不比标准答案、不比开源基线，而是"输入里可量化的内容有没有活进输出"（守恒）。守恒 + 内在合法性同一次解析，既给 pass/fail，又长出 behavior_signature。两条纪律不能破——**输入侧用独立抽取器测**（防循环）、**守恒看数据不看格式**（容差异化价值）；基线只当薄壳探针、绝不当正确性标尺。这一环立住，契约引擎的"目的内"判定才有客观输入。
